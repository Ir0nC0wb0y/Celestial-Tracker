// Possible functions
class Homing {
// Public
  // bool isHomed()
  // this may not be necessary if this is only done during setup

  // void goHome()
  // initiates the homing sequence

  // void runHoming()


// Private
  //bool _limitTouched = false;
  //int _homingStateAz = -1; //used for state machine
  //int _homingStateEl = -1; //used for state machine
  // state -1 = homing class initialized, not run
  // state  1 = turning constant speed
  // state  2 = limit switch engaged, move back ## steps
  // state  3 = turing constant speed, *slower*
  // state  0 = homed
  
  
  
  
  //void _interruptAz()
  //void _interruptEl()
}
